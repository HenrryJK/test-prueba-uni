


export function mensaje(nombre: string) {
  // la funcion funcion correctamente y no se dispara la prueba
  return `Saludos ${nombre}`;  /// Saludos Henrry

    /// estariamos retornando un Booleano y no un string
    /// La prueba dispara el it() y el mensaje indicando el error.
    //  return true;
}
